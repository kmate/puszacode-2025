# Day 10 — Find Maximum in Array

Array exploration continues! 📈

Write a function called `findMax` that takes an array of numbers and returns the largest number.

**Example:**
```javascript
findMax([1, 5, 3, 9, 2]) → 9
findMax([10, 20, 15]) → 20
findMax([7]) → 7
```

**Your task:**
Create the function and find `findMax([34, 67, 23, 89, 12])`. The unlock code is this number.

**Hint:** Use `Math.max()` with spread operator or loop through the array!
