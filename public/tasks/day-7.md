# Day 7 — Array Sum

Welcome to arrays! 📚

Write a function called `sumArray` that takes an array of numbers and returns their sum.

**Example:**
```javascript
sumArray([1, 2, 3]) → 6
sumArray([10, 20]) → 30
sumArray([5]) → 5
```

**Your task:**
Create the function and calculate `sumArray([7, 8, 9])`. The unlock code is this result.

**Hint:** Use a loop or the `.reduce()` method!
