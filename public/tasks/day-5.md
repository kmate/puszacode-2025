# Day 5 — String Length

Let's explore strings! 📝

Write a function called `getLength` that takes a string and returns its length.

**Example:**
```javascript
getLength("hello") → 5
getLength("code") → 4
getLength("") → 0
```

**Your task:**
Create the function and find `getLength("PuszaCode")`. The unlock code is this number.

**Hint:** Strings have a `.length` property!
