# Day 17 — Remove Duplicates

Clean up that array! 🧹

Write a function called `removeDuplicates` that takes an array and returns a new array with duplicate values removed.

**Example:**
```javascript
removeDuplicates([1, 2, 2, 3, 3, 3]) → [1, 2, 3]
removeDuplicates([5, 5, 5]) → [5]
removeDuplicates([1, 2, 3]) → [1, 2, 3]
```

**Your task:**
Create the function and process `[7, 3, 7, 3, 5]`. The unlock code is the sum of the unique numbers.

**Hint:** Use a Set or filter with indexOf!
