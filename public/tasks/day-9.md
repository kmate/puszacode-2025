# Day 9 — Reverse String

Time to flip things around! 🔄

Write a function called `reverseString` that takes a string and returns it reversed.

**Example:**
```javascript
reverseString("hello") → "olleh"
reverseString("code") → "edoc"
reverseString("abc") → "cba"
```

**Your task:**
Create the function and reverse "STAR". The unlock code is this result (in uppercase).

**Hint:** Convert to array, reverse it, then join back to string!
