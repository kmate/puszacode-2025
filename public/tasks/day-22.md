# Day 22 — Anagram Checker

Are they anagrams? 🔤

Write a function called `areAnagrams` that takes two strings and returns `true` if they're anagrams (contain the same letters), `false` otherwise.

**Example:**
```javascript
areAnagrams("listen", "silent") → true
areAnagrams("hello", "world") → false
areAnagrams("triangle", "integral") → true
```

**Your task:**
Create the function. If `areAnagrams("earth", "heart")` is true, the code is "ANAGRAM", otherwise "NOTANAGRAM".

**Hint:** Sort the letters of both strings and compare!
