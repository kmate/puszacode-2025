# Day 8 — Count Vowels

Let's count characters! 🔤

Write a function called `countVowels` that takes a string and returns the number of vowels (a, e, i, o, u) in it.

**Example:**
```javascript
countVowels("hello") → 2
countVowels("sky") → 1
countVowels("rhythm") → 0
```

**Your task:**
Create the function and count vowels in "Christmas". The unlock code is this number.

**Hint:** Loop through the string and check each character. Case-insensitive is fine!
