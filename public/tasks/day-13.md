# Day 13 — Factorial

Let's calculate factorials! 🎲

Write a function called `factorial` that takes a number n and returns n! (n factorial = n × (n-1) × (n-2) × ... × 1).

**Example:**
```javascript
factorial(5) → 120  // 5 × 4 × 3 × 2 × 1
factorial(3) → 6    // 3 × 2 × 1
factorial(1) → 1
factorial(0) → 1
```

**Your task:**
Create the function and calculate `factorial(6)`. The unlock code is this result.

**Hint:** Use a loop or recursion!
