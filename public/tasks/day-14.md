# Day 14 — Palindrome Length

Find the longest palindrome! 🔄

Write a method called `longestPalindromeLength` that takes a string and returns the length of the longest palindromic substring(case-insensitive, spaces ignored).

**Example:**
```java
longestPalindromeLength("racecar") → 7
longestPalindromeLength("abcba") → 5
longestPalindromeLength("hello") → 2  // "ll"
```


**Your task:**
Create the method and find the longest palindrome length in "A Santa at NASA". The unlock code is this result.

**Hint:** Check all substrings, test each if it's a palindrome, track max length!
