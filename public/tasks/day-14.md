# Day 14 — Palindrome Checker

Is it the same forwards and backwards? 🔄

Write a function called `isPalindrome` that takes a string and returns `true` if it reads the same forwards and backwards, `false` otherwise.

**Example:**
```javascript
isPalindrome("radar") → true
isPalindrome("hello") → false
isPalindrome("level") → true
```

**Your task:**
Create the function. If `isPalindrome("racecar")` is true, the code is "RACECAR", otherwise "NOTPALIN".

**Hint:** Compare the string with its reverse!
