# Day 4 — Maximum Value

Time to compare numbers! 📊

Write a function called `max` that takes two numbers and returns the larger one.

**Example:**
```javascript
max(5, 10) → 10
max(100, 50) → 100
max(7, 7) → 7
```

**Your task:**
Create the function and find `max(88, 99)`. The unlock code is this result.

**Hint:** Use an `if` statement or the ternary operator `? :`!
