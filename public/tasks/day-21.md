# Day 21 — Flatten Array

Flatten nested arrays! 📦

Write a function called `flatten` that takes an array (possibly containing nested arrays) and returns a flat array with all values.

**Example:**
```javascript
flatten([1, [2, 3], 4]) → [1, 2, 3, 4]
flatten([[1, 2], [3, 4]]) → [1, 2, 3, 4]
flatten([1, [2, [3, 4]]]) → [1, 2, 3, 4]
```

**Your task:**
Create the function and flatten `[5, [6, 7], [8]]`. The unlock code is the sum of all numbers.

**Hint:** Use `.flat()` method or recursion!
