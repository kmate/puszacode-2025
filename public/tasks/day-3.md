# Day 3 — Even or Odd?

Let's learn about conditions! 🎯

Write a function called `isEven` that takes a number and returns `true` if it's even, `false` if it's odd.

**Example:**
```javascript
isEven(4) → true
isEven(7) → false
isEven(0) → true
```

**Your task:**
Create the function. The unlock code is: if `isEven(42)` is true, use "EVEN42", otherwise use "ODD42".

**Hint:** Use the modulo operator `%` to check if a number divides evenly by 2!
