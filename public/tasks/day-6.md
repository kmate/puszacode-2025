# Day 6 — Repeat String

Time for string manipulation! 🔄

Write a function called `repeat` that takes a string and a number, and returns the string repeated that many times.

**Example:**
```javascript
repeat("Ho", 3) → "HoHoHo"
repeat("*", 5) → "*****"
repeat("Hi", 1) → "Hi"
```

**Your task:**
Create the function and calculate `repeat("Joy", 2)`. The unlock code is this result.

**Hint:** Use the `.repeat()` method or a loop!
